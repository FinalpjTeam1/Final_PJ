<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
-<mapper namespace="com.heal.dao.MemberDao">

<!-- public String login(String memberId, String memberPw); -->


<select parameterType="map" resultType="String" id="login">select gradefrom memberwhere id = #{id} and pw = #{pw} </select>

<!-- public Member loginToMember(String memberId, String memberPw) -->


<select parameterType="map" resultType="com.heal.dto.Member" id="loginToMember">select *from memberwhere id=#{id} and pw=#{pw} </select>

<insert parameterType="map" id="insertMember">Insert into member(id, pw, nick, mobile, gender, age)VALUES(#{id}, #{pw}, #{nick}, #{mobile}, #{gender}, #{age}) </insert>

<select resultType="com.heal.dto.Member" id="idCheck">SELECT*FROM MEMBERWHERE id= #{test00001} </select>

<select id="updateMember">UPDATE memberSET mobile = #{mobile} , profile = #{profile}WHERE id = #{id} </select>

</mapper>








